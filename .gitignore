node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# git internals accidentally committed once
FETCH_HEAD

# runtime upload storage (contract attachments etc.) — data, not source
uploads/

# Replit paste-artifact binaries (screenshots, zips); text/code artifacts stay tracked for now
attached_assets/*.png
attached_assets/*.jpg
attached_assets/*.jpeg
attached_assets/*.zip
attached_assets/*.pdf
