node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# git internals accidentally committed once
FETCH_HEAD

# runtime upload storage (contract attachments etc.) — data, not source
uploads/

# Replit paste-artifact dump (screenshots, zips, code/text pastes) — dev detritus, not source
attached_assets/
