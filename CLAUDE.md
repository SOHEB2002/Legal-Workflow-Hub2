# Project: Legal Workflow Hub 2 (oun-law.com)

## What this is
Saudi law firm workflow platform (Arabic RTL UI). Production on a Replit Reserved VM at oun-law.com.
Stack: TypeScript + React + Tailwind + Shadcn/UI (frontend), Node/Express (backend), PostgreSQL (Neon) + Drizzle ORM, JWT + CSRF auth. GitHub: SOHEB2002/Legal-Workflow-Hub2.
10 roles across 4 departments. All future changes must stay mobile-app-compatible (PWA + Capacitor).

## CRITICAL — Two databases
- Workspace `$DATABASE_URL` = DEVELOPMENT (heliumdb).
- PRODUCTION is separate (neondb) — NEVER touch without explicit user approval.
- Replit Republish diffs dev-DB vs prod-DB schemas and can generate DESTRUCTIVE migrations if they drift. Dev/prod schema drift caused a near-miss `DROP COLUMN follow_up_count` incident.
- NEVER suggest "Copy database to production".
- Schema changes: **ADDITIVE ONLY** — new nullable columns OK; widening a TS type to match an already-nullable column OK; anything needing a migration = STOP and ask. (Schema-rule reflex from replit.md: never delete/reset/modify existing data.)

## CRITICAL — Operational rules
- **NEVER run `npm run dev`** — burns Replit credits (caused a real $72 overage). `npx tsc --noEmit` / `npm run check` are fine and are the standard gate.
- PowerShell 5.1: single-quoted here-strings break on embedded quotes/backticks → **ALWAYS `git commit -F <msgfile>`** (write temp file under `.git/`, commit, delete).
- Files have CRLF line endings — scripts must tolerate trailing `\r`.
- BiDi rendering reverses Arabic char order in diff viewers — verify actual bytes before "fixing" Arabic string order.
- Always show diff + verification before pushing.
- Deploys happen from the **Replit Shell** (merge feature → main, `--no-ff`), NOT from this machine. Replit appends empty "Published your App" checkpoints to main after each publish — feature will show "diverged" from main; this is benign, ignore it.

## Workflow & division of labor
- The user (Soheb) is non-technical, Arabic-speaking. He relays prompts from a planning Claude session; reports go back to that session for review.
- **MOBILE-SESSION REPORTING:** When the user is working via Remote Control from mobile, the final report is the ONLY thing relayed to the planning session — intermediate steps can't be copied. So final reports must be self-contained: include commit hashes + per-commit summary (files touched, +/- line counts), all gate results (tsc, noUnusedLocals, custom checks), every judgment call made with its rationale, anything that STOPPED or deviated from plan, and exact verification commands the planning session could request if it wants to drill in (e.g. `git show <hash> --stat`). Write it as if the reader saw nothing of the session.
- Branch: **`feature/consultations-audit`** (long-running audit branch). Never commit to main from here.
- **Operating mode — "investigate-and-apply-if-routine" (since Phase 2):**
  - For batches on the PROVEN pipeline (cast cleanup, type-only, mechanical): investigate → if all findings are within the proven pattern (vestigial drops, precise narrowings, tsc stays 0, zero behavior change, zero schema change) → APPLY, COMMIT, PUSH directly, then report.
  - **MUST STOP and report WITHOUT applying** if ANY of: a new bug discovered; anything touching `shared/schema.ts` beyond additive type-widening of already-nullable columns; any behavior change; any permission-logic change; anything needing a DB migration; any ambiguous judgment call.
  - Deploys to production are ALWAYS user-driven — never merge to main or advise Republish without explicit instruction.

## The proven cast-cleanup pipeline
1. **Inventory:** grep counts + categorize (VESTIGIAL / TYPE_MISMATCH / FIELD_NOT_ON_TYPE / MAP_INDEX / LEGITIMATE).
2. **Controlled experiment:** strip casts → `npx tsc --noEmit` → capture surfaced errors → revert.
3. Vestigial (no error) → delete. Value mismatch → precise type (`as ProperUnion`). Map-index → `as Record<string,string>`. Field-not-on-type → **DIAGNOSE** (could be a bug — e.g. the `assigned_to` NOT NULL finding). Legitimate (`catch (e: any)`, multer `req.file`, raw JSON `any[]`) → keep.
4. NEVER re-add blanket `as any`. NEVER relocate a cast to silence an error.
5. **Gates before commit:** tsc = 0; grep verification counts; `git diff | grep "^+" | grep "as any"` shows only precise casts; behavior unchanged.
6. Commit messages document findings (root fixes like `role: UserRoleType`, `PriorityType`) so diffs self-explain.
- **Vestigial-cast philosophy:** ~90% of `as any` is stale residue from the additive-only schema — `LawCase` (and sibling interfaces) already have the fields the casts strip (`isArchived`, `clientRole`, `grievanceRequired`, najiz/mohr/taradi fields, etc.). Removing them INCREASES safety by re-enabling typo detection. Verify each field against the relevant `shared/schema.ts` interface before removing.
- **Transient-API-field pattern:** request-body fields that are NOT entity columns (e.g. `startingStage`, `stageChangeNotes`) use `as Partial<LawCase> & { field?: T }`, NOT `as any`.

## Type & permission rules
- Permission logic must NEVER change in cleanup batches (type-only). Phase 5 handles auth/permissions deliberately.
- `AuthRequest.user = { id, role: UserRoleType, name, departmentId }` — populated by authMiddleware/requireAuth.
- Known deferred items live in the memory files (assigned_to NOT NULL bug, Date-mode timestamp casts, etc.) — check memories before re-investigating.

## Key files
- `server/routes.ts` — all API endpoints (~3200+ lines).
- `server/storage.ts` — `DatabaseStorage` class, all DB ops (~4,489 LOC, ~122 `as any` — next major target).
- `shared/schema.ts` — Drizzle tables, Zod schemas, TS interfaces (`LawCase` ~2017-2096).
- `server/auth.ts` — JWT + CSRF. `server/scheduler.ts` — background jobs.
- `queryClient.ts` — `getAuthHeaders`, single-flight `refreshAuthToken`, `apiRequest` (the unified read path). `lib/utils.ts` — `extractApiError`.

## Resolved bug (history — see Phase-3 3E)
- **memo `assigned_to` NOT NULL silent failure — FIXED (commit fa45c6f):** dept-transfer used to write `assignedTo: null` to the NOT NULL column; Postgres threw, the try/catch swallowed it, memos silently stayed with the old lawyer. Now writes `""` (the unassigned sentinel); the primaryLawyerId-change cascade re-points memos when the new dept assigns a lawyer. (Note: *consultations*.assigned_to IS nullable — source of the original mis-read.)

## Current state (update this section as work progresses)
- **Phase 0 + 1 (frontend):** complete, deployed. Frontend casts → 0: cases 107→7 (7 intentional/tracked), consultations→0, hearings→0, notifications→0 (root-fixed via `NotificationResponse` widening in schema.ts). Batch 3 cleared all 17 TS errors → **tsc-0 baseline (regression check: must stay 0)**.
- **Phase 2 (routes.ts):** complete, deployed (2A-2C merged to main via `--no-ff`). 282→~17 casts = permanent (`catch`/`error as any` + multer `req.file`); the 2 deferred Date casts resolved in 3C.
- **Phase 3 (storage.ts):** IN PROGRESS, branch only (NOT deployed). 122→107 casts so far.
  - **3A probe** ✅ — verdict: the ~95 Drizzle write-payload casts (`.set/.values({…}) as any`) are VESTIGIAL/cargo-culted, not load-bearing. 3D is a mechanical sweep, not an architectural project. (Root: bare `timestamp()` = Drizzle date-mode vs string interfaces; values actually match `$inferInsert`.)
  - **3B** ✅ commit 57da4b0 — 15 vestigial field-access casts stripped (122→107).
  - **3C** ✅ commit 5eab4e3 — resolved the 2 deferred Date casts; extended the closedAt string→Date idiom to `followUpStartedAt`/`expectedDeliveryDate` in `updateConsultation`+`updateConsultationAndLog`; routes passes `.toISOString()`.
  - **3D** ✅ commit dad874a — swept 105 write-payload casts per-section with tsc gates. 104 vestigial; 1 load-bearing (`createSupportTicket` spread `Partial<SupportTicket>` into `.values()`, masking 4 notNull-no-default columns) → root-fixed by tightening the signature with `Pick<…>`, no cast. **storage.ts: 2 casts left, both legitimate** (`err as any` :43, `(e as any)?.message` init).
  - **3E** ✅ commit fa45c6f (approved behavior change) — dept-transfer memo unassign now writes `""` (the system's unassigned sentinel: auto-memos use `primaryLawyerId || responsibleLawyerId || ""`; server guards `!!memo.assignedTo`; UI sorts unassigned first) instead of the null that violated NOT NULL and was silently swallowed. The existing primaryLawyerId-change cascade (routes ~2449) completes "memo follows its case" when the new dept assigns a lawyer. No migration. Known low-priority gap (backlog): cascade doesn't fire on responsibleLawyerId-only assignment.
- **Phase 6 (dead code & deps):** COMPLETE, 6A-6G all applied & pushed. Dead deleteUser methods + date-utils exports + **all 134 unused imports/locals** removed — `tsc --noUnusedLocals` is now ZERO project-wide (use as regression gate). attached_assets/ + uploads/ + FETCH_HEAD fully untracked (cached-only) and gitignored. 16 unused deps removed (74→62; legacy passport/session stack etc.), `npm run build` gate passed — **lesson: `@types/pg` had to be added explicitly, it was riding on removed `@types/connect-pg-simple`**. Multipart upload refresh race fixed (aa06f16): contracts.tsx now uses queryClient's single-flight `refreshAuthToken` — last hand-rolled /api/auth/refresh outside queryClient/auth-context.
- **Phase 4 — 2D' validation hardening:** COMPLETE & pushed (branch only, not yet deployed). All **70 mutating routes that read a JSON body** now carry tolerant zod gates: V1 13 Tier-1 (c9904ed Pattern-B + bf94a75 Pattern-A), V2a 21 cases/consultations Tier-2 (27c9d7a + 30441bd), V2b 30 contracts/memos/misc Tier-2 (5137332 + a0f6f57 + 68576fd), V3 6 Tier-3 (633549a). Method = the `validation-patterns` skill: `.passthrough()` always, all-optional gate-only schemas (handlers keep their own Arabic 400s → zero behavior change), nullability mirrored from entity interfaces, no enum narrowing, FE-contract enumerated per route, gate placed before the first `req.body` read. 5 shared workflow body-shapes (workflowTargetStage/Decision/Outcome/Reason/Notes) reused across all four resources (approved decision c, not the declined 2E handler dedup). **Out-of-scope by design:** 23 no-body mutating routes (param-only) + 2 multipart/multer routes (legitimate raw). **Still open:** (1) court-register — **DECISION (2026-06-13): DELETE** the dialog (cases.tsx ~2932-2950 + showCourtRegistrationDialog/courtRegistrationCaseId/courtCaseNumberInput state) + the `POST /api/cases/:id/court-register` route + the unused `courtRegisterCaseSchema`. Rationale: the live stage-bar najiz path auto-promotes `قيد_الدراسة → منظورة_بالمحكمة` (routes.ts:2049-2059) and fully covers court promotion; the user confirmed no workflow needs court-register's `تحت_النظر` landing (the `منظورة` landing always suffices); and the dialog has been broken since the server `najizNumber` requirement was added (sends only `{courtCaseNumber}` → guaranteed 400). To be executed in the next on-machine batch alongside Phase 4 Batch S. (2) Phase-5 F1 notification field-allowlist + F2 reset-password-route removal (memory: phase5-auth-backlog).
- **Phase 4 Batch S (safe-additive schema audit):** **S1 ✅ DONE (7370f4b, pushed, branch only — NOT yet deployed):** 16 hot-path indexes added on FK-like filter columns mirroring the contracts idiom — lawCases(department/primaryLawyer/responsibleLawyer/createdAt), consultations(department/assigned/createdAt), and a caseId index on memos/hearings/caseNotes/caseComments/caseActivityLog/legalDeadlines/fieldTasks + notifications(recipient) + contactLogs(client). Index-only diff (48+/10−, the 10 removed lines all `});`→`}, (t)=>({…}))`); zero column/type/constraint change; 16 names verified collision-free (40 total index() decls). JSONB assignedLawyers GIN skipped (separate). Gates: tsc 0, noUnusedLocals 0. **DEPLOY DIFFERS from the zod-only batches: Republish WILL show 16 CREATE INDEX statements (expected, additive); any DROP = not this batch → stop.** **S2 ✅ DONE (2f4af1c, pushed):** code-only perf — getNextTicketNumber now a single `COALESCE(MAX(CAST(SUBSTRING(ticket_number FROM '[0-9]+') AS INTEGER)),0)` aggregate (was load-all + JS reduce); deleteCase notification/attachment DELETEs batched into `relatedId IN (…)` (was N+1), `length>0` guards kept. Zero behavior change. **S3 ✅ DONE (2ec2f1e, pushed) — found + fixed 1 real bug:** date-mode WRITE-path audit. `archivedAt` was the only raw-string→date-mode leak: the auto-archive scheduler passed `now.toISOString()` (string) to updateCase, which converts closedAt but NOT archivedAt → drizzle's `value.toISOString()` threw on the string → swallowed by the scheduler try/catch → **auto-archive of >6-month-closed cases was fully broken**. Fixed by applying the closedAt idiom to archivedAt in updateCase (single chokepoint) + dropped the vestigial `as any` at the scheduler call site. Every other date-mode field verified correct (pausedAt/completedAt/readAt/scheduledAt/editedAt/approvedAt/resolvedAt/submittedAt/reviewedAt/startedAt/expectedDeliveryDate — idiom applied or server-Date). **S4 PARTIAL:** notifications senderId/senderName drift ✅ DONE (7c7fec6, pushed) — interface narrowed `string|null`→`string` to match the NOT-NULL columns + `||""` storage coalesce (type-only). **Stale Delegation interface → RESOLVED: client delegation system DELETED (51ac66e + af3d661, pushed) — Option A.** Investigation found the `Delegation` interface typed a SECOND, parallel delegation system: a client-side localStorage feature (users-context.tsx state from `localStorage["user_delegations"]` + DelegationDialog from users.tsx/user-profile.tsx + a التفويضات profile tab) that was per-browser, unshared, enforced nothing (`canActOnBehalf` had zero consumers), yet showed a fake "تم إنشاء التفويض بنجاح" success toast — a confirmed UX trap, strictly inferior to and redundant with the real server system. DELETED entirely (dialog file + all client surface + the stale interface + `ExtendedUser.activeDelegations`); resolves S4's rename question (deleted, not renamed). KEPT generateId/DelegationType/DelegationTypeValue (vacation system uses them). SERVER system (DelegationRecord, /api/delegations, delegations.tsx, scheduler checkDelegationExpiry, deactivation guard) 100% untouched — `/delegations` is now the only way to delegate. Gates tsc 0 / noUnusedLocals 0. See [[delegation-systems]]. Batch M (FK constraints, dead-column drops) is MIGRATION-REQUIRED → deferred to an explicitly-approved two-DB session. Full findings in the Phase-4 schema-audit report.
- **Phase 8 Batch P8-1 — DONE (796e687, pushed):** deleted court-register (dialog+state+route+`courtRegisterCaseSchema`) and the broken تراضي-status outcome buttons (F1) — both "court-register class" dead paths superseded by the live stage-bar/مداولة_الصلح flow. KEPT the تراضي register button+badge (sets `taradiNumber`, read by the stage flow). **F2 (viewer) WITHDRAWN as a false positive:** `server/index.ts:51-59` has a `viewerWriteGuard` that 403s every non-GET from a viewer before any handler — viewer write is already blocked; the viewer entry in `canModify*`/`canActOnHearing` is intentional + documented (routes.ts:190-196) for READ access and must stay. (My earlier Conflict-2 analysis erred by grepping `auth.ts` instead of `index.ts`.)
- **Phase 8 Batch P8-A — DONE (70d2f18, pushed):** consultations dept-scope parity — F4 (/assign), F6 (/early-close), F9 (/return-to-committee) now scope `department_head` to own department, mirroring sibling shapes verbatim. **F8 (pause/unpause/skip-completion null-guard): DECISION (2026-06-13) = LEAVE AS-IS.** consultations is already sibling-consistent (cases /pause + `allowContractPauseLike` both use the same simple form; skip-completion siblings disagree; the two-sided null-guard exists only in contracts /early-close, a different action). Adding it consultations-only would create new divergence, not parity. If pause-family null-guard hardening is ever wanted, do it across ALL resources together as a deliberate separate batch.
- **Phase 8 Batch P8-C — DONE (9876f4b, pushed):** cosmetic F14 (awaitCaseCompletion uses `CaseStage.DATA_COMPLETION` enum, byte-identical refactor) + F15 (corrected the consultations.source default comment to `عبر_المجموعة`).
- **Phase 8 deferred decisions:**
  - **F7 (legacy memo review panel, memos.tsx ~1916): RESOLVED — DELETED (49d2e9d, pushed, 2026-06-13).** Prod count of `memos WHERE current_stage IS NULL` is now **0** (verified against prod/neondb): the user deleted the last 5 legacy null-stage memos through the normal platform UI (the app's own delete path cleaned up notifications/activity-log). Since createMemo has set currentStage on every memo since Phase 9 and the panel was gated SOLELY on `!detailMemo.currentStage`, it can never render → genuine dead code. Removed 84 lines: the panel JSX + its only-callers `handleApprove`/`handleReturn` + `reviewNotes` state + the view-details `setReviewNotes("")` reset + orphaned `canReviewMemos`/`XCircle` imports. KEPT the ~1852 status badge (its `|| CANCELLED` arm stays live) and `handleStatusChange` (used by live DRAFTING/IN_REVIEW/SUBMITTED buttons). Gates: tsc 0, noUnusedLocals 0, zero `as any`, zero prod behavior change.
  - **F12 (admin_support return-to-committee policy divergence — contracts excludes admin_support, the other 3 allow it): DEFERRED.** The `admin_support` role is slated for a major structural change; its permission policies will be reviewed within that work — do NOT touch F12 now.
- **Phase 8 — Business-Logic & Contract Audit (NEW, planned):** a DEDICATED review pass — distinct from the type/cast/dead-code cleanup audits — whose EXPLICIT goal is hunting FE↔server contract mismatches, broken/dead UI (buttons that can never succeed, like court-register), redundant conflicting paths (two mechanisms doing one job, one live one divergent), contradictory entity assumptions (NOT NULL vs nullable, `""` vs null sentinels, status/stage-value mismatches across the 4 resources), and permission-logic inconsistencies between sibling routes. Use `audit-method` skill discipline. **Rationale:** all ~12 bugs found so far (assigned_to NOT NULL, createSupportTicket masking, court-register, etc.) were caught as SIDE-EFFECTS of type/validation work — Phase 8 makes contradiction-hunting the primary objective rather than a lucky by-product. Investigation report (the fix plan) produced 2026-06-13.
- **FEATURE phase — DEFERRED until ALL repair/audit work is complete (decision 2026-06-13):** the planned feature work — the commit-bypass button (`تجاوز لجنة المراجعة`), F3 contracts return-to-committee UI, and F11 memo mandatory-notes — is an **ADDITION, not a fix**. The user wants every repair/audit fix FINISHED and REVIEWED before any feature work starts. **Do NOT start the feature phase until explicitly told.** It will be the **first use of the `feature-design` skill (build it BEFORE coding the feature) and later `feature-verify` (after it ships).** This decision intentionally sequences all of Phase 4/8 repair ahead of net-new capability.
- **Phase 7 — Performance & Security audit (investigation 2026-06-13; full plan in memory: [[phase7-security-perf-plan]]):** **P7-1 ✅ DONE (pushed, branch only):** S-1 (71fc217 — redact /api/auth/* response bodies in the index.ts logger; was logging token/csrfToken/tempPassword), S-7 (878ce4e — global error handler returns generic 5xx instead of raw err.message; 4xx keep theirs; only unhandled throws reach it), P-1 (7b3bb07 — bulk markAllNotificationsRead single-UPDATE replaces the mark-all-read N+1; identical outcome). All code-only, gates tsc 0 / noUnusedLocals 0. **P7-2 ✅ DONE (local commits 94ae650/43f5b89/b5d6ff3 — NOT yet pushed):** P-3 all 5 indexes added (94ae650 — attachments(entityType,entityId), notifications(createdAt), memos(deadline), hearings(hearingDate,hearingTime), lawCases(updatedAt) with createdAt kept; mirror S1, Republish shows 5 CREATE INDEX); P-4 dashboard unread-count → getUnreadNotificationCount COUNT(*) (43f5b89 — cases/hearings/memos counts INTENTIONALLY left as-is: their full loads are required to build the endpoint's returned alerts/lists); S-5 deleted both dead attachment routes + orphaned multer config (b5d6ff3). **Remaining by class:** 🟡 npm non-breaking `audit fix` only (never --force; 23 vulns 11mod/12high; needs build-gate). 🟠 needs-decision: **S-2 MASTER_PASSWORD = user chose to KEEP (deliberate dev/break-glass tool — do NOT re-flag)**, S-3 emergency-reset secret reuse, drizzle/esbuild major bumps (not real exploit paths), P-2 search ILIKE. 🔵 behavior-change: S-4 helmet/CSP, S-6 WS-token transport, P-2 search, P-5 list pagination (FE/Capacitor impact). Verified clean: SQL fully parameterized, multer hardened, secrets from env, all routes authed, bundle size a non-issue.
- **Deferred / pending:**
  - **Delegation — server system does NOT enforce delegated case ACCESS** (memory: [[delegation-systems]]). The real `/api/delegations` system records + notifies + guards user-deactivation, but no code grants a delegate access to the delegator's cases (`getActiveDelegationsForUser` storage ~3755 has zero callers; `auth.ts` never checks delegations; the "cases returned to you" expiry notice is informational). **FUTURE FEATURE (user's intent, feature-design track, NOT a repair):** make delegation enforce real access + wire into the unified tasks page so all of a delegator's tasks transfer to the delegate for the window. Minor server dead code for a future sweep: `getActiveDelegationsForUser` (unused).
  - Cascade gap (low): lawyer assignment setting only `responsibleLawyerId` doesn't re-point memos (cascade keys off `primaryLawyerId`).
  - Phase 5 auth pass backlog (memory: phase5-auth-backlog): `fetchUsersFromAPI` migration; instant-revocation wire-up (user deactivation takes ≤30s to propagate via activeUserCache TTL — 2-line fix if wanted).
  - **Skills roadmap — 2 remaining, build each WHEN its trigger arrives (user decision, do not build preemptively):**
    - `validation-patterns`: ✅ DONE (ecccde7) — extracted from the shipped 2D' V1 batch.
    - `feature-design`: BEFORE the first new feature — needs an architectural inventory pass (tables, notification/permission/workflow systems, declined decisions) to give new features a design-first checklist.
    - `feature-verify`: AFTER the first new feature ships — capture the manual test checklist pattern (gates + edge cases + Arabic user-test steps) from that real experience.
    - When any of these milestones is reached, REMIND the user that the corresponding skill is due.
- **Explicitly DECLINED (not deferred) — revisit only if a test suite is adopted:**
  - `useCasesFilter` hook extraction (Batch 4D). cases.tsx structural-extraction work ended at Batch 4B.
  - **2E workflow dedup**: the ~40 near-duplicate workflow handlers carry deliberate per-resource differences (stages, notifications, side-effects); DRY-ing them without tests is high risk on the system's most critical logic for cosmetic gain. The duplication is readable, and isolation is a feature — editing consultation workflow can't break contracts.
